"""Map player names across stat sources."""

import pandas as pd

def unify(fg_file: str, bp_file: str, output_file: str) -> None:
    """Placeholder function to merge player lists."""
    # Implementation will map IDs across sources
    pd.DataFrame().to_csv(output_file, index=False)

if __name__ == '__main__':
    print('Unify players script placeholder')
